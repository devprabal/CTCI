/**
 * @brief Initializes space for an integer square matrix (2-D array) in memory.
 *
 * @details
 *  It just creates the integer matrix without initializing any of its elements.
 *  The matrix is an array of integer pointers.
 *
 * @param n the number of rows or columns
 * @return int** returns a pointer to the created 2-D array
 */
int **createSqrMatrix(int n);