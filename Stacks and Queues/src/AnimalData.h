/*
This header file is placed in src dir because it is not as important (one specific program only depends on it) as the other headers.
*/
#ifndef ANIMAL_DATA
#define ANIMAL_DATA
struct AnimalData {
  int timestamp;
  char type;
};
#endif