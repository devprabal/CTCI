#include "Node.h"
/**
 * @brief does what you think it should do, prints out the linked list entirely
 * @details you will need to specify the numberOfElements if the linked list is cyclic.
 *
 * @param head pointer to the start of the linked list
 * @param numberOfElements number of nodes to print in the list, default is -1 to print all the nodes until NULL
 */
void printLinkedList(Node * head, int numberOfELements=-1);